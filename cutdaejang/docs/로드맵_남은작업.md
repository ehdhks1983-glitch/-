# 컷대장 남은 작업 로드맵 (v0.3 기준, 2026-07-12)

> 📌 **콘텐츠 품질·경쟁 대응 로드맵**(2026-07-23, v0.75~v0.77 제안)은
> [경쟁분석_알파컷_대응로드맵.md](경쟁분석_알파컷_대응로드맵.md) 참고 — P1 훅 팩 / P2 b-roll / P3 템플릿 갤러리.

## 🐞 알려진 버그 (사용자 리포트 — 다음 릴리스 후보)

- [x] **내 제품 정보가 "안 골랐을 때도" 계속 반영됨** — ✅ v0.77 (방향 ②+α:
  생성 버튼 위 배지 + [✕ 제품 끄기] + 선택 즉시 저장으로 빈 값도 기억)
  - 증상: 제품을 한 번 등록·사용하면, 이후 제품을 고르지 않았다고 생각해도
    영상이 계속 그 제품 정보 기준으로 나옴.
  - 원인(확인): AI 영상 폼의 `genProductSel`이 페이지 로드 때마다 마지막 사용
    제품(`settings.ui.gen_product`)으로 자동 복원됨 (webui.py 상태복원 ~6434행:
    `gsel.value = lastP`). "없음 (일반 주제)" 옵션은 있지만 자동으로 그리 돌아가지
    않음. 게다가 「📇 내 제품 정보」 관리 카드의 `prodSel`(편집용)과 폼의
    `genProductSel`(실제 대본 근거)이 분리돼 있어, 관리 카드가 "새 제품…"이면
    제품이 꺼진 걸로 착각하지만 생성은 기억된 제품을 계속 씀.
  - 백엔드는 정상: `_product_context()`는 `product`가 비면 아무 것도 주입 안 함
    (webui.py 228행) — 순전히 프런트 자동복원/UI 혼동 문제.
  - 수정 방향(택1): ① gen_product 자동복원 제거(기본 "없음") / ② 제품이 선택된
    상태면 폼에 눈에 띄는 배지·경고 표시 / ③ 관리 카드와 폼 선택을 하나로 통합.

## ① 사용자 검증 대기 — 이 결과가 다음 개발 순서를 정한다

- ~~캡컷 draft 열림 확인~~ → v0.41에서 캡컷 기능 자체를 제거 (사용자 결정)
- [ ] Gemini 실키 4종: 15문장 완주(429 없이) / 재생성 캐시(즉시) / 말투 3종 미리듣기 / 지시문 발화 여부
- [ ] Gemini 배경 이미지 생성 동작 확인 (실패 시 모델명 교체 필요)
- [ ] GPU 노트북 nvenc 감지 + 60초 쇼츠 렌더 시간 실측
- [ ] TTS 비용 기록 (60초 1편당) → 판매가 책정 근거 (기획안 §11-3)
- ~~CapCut 버전 기록~~ → 캡컷 기능 제거로 불필요

## ② v1.0(판매 가능) 필수 개발 — 우선순위순

1. [x] API 키 저장 옵션 — ✅ v0.4 ("이 PC에 저장" 체크박스 + 키 삭제)
2. [x] 발음 치환 버튼 — ✅ v0.4 (검토 화면 [한글 발음으로 변환], utils/pronounce)
3. [x] 히스토리 재생성 버튼 — ✅ v0.4 (♻ 재생성 — 저장된 spec으로 mp4 재렌더)
4. [x] 설정 화면 — ✅ v0.4 (⚙ 설정 카드: 자막/줌/BGM/간격/한도, settings.json 저장)
5. [x] 오류 로그 파일 + 진단 리포트 — ✅ v0.4 (logs/cutdaejang.log + 🩺 진단 리포트 저장)
6. ~~캡컷 캘리브레이션~~ → v0.41에서 캡컷 기능 제거
7. [ ] .exe 설치본 — PyInstaller onedir + Inno Setup, FFmpeg(libass)·폰트 동봉 (Phase 3, Windows 빌드)
8. [ ] 통합 QA: A/B 출력 × 검토/자동 4조합 (Phase 3)

## ③ 이후 확장 (기획안 v1.5 / v2.0)

- [x] **육성 촬영 영상 모드 (STT + 무음컷)** — ✅ v0.5 (내 영상 편집: 무음컷+자동자막, 쇼츠/원본비율)
- [x] 일반(16:9) 모드 — ✅ v0.61 AI 생성 가로 롱폼 + v0.77 편집 모드 「가로 (16:9)」(블러 패드)
- [ ] 편집 모드 자막 검토·수정 화면 (자동 인식 자막 손보기) ← 다음 우선
- [ ] 긴 영상 → 하이라이트 쇼츠 여러 개 (v2.0 — 사용자 요청 2순위)
- [x] 배치 모드: 주제 여러 줄 → mp4 N개 연속 — ✅ v0.42 (생성 폼 「여러 개 한 번에」)
- [ ] 외부 TTS mp3 임포트 (whisper 정렬)
- [x] **블로그 글 링크 → 사진 영상** — ✅ v0.78 (글·사진 자동 수집 + AI 대본 요약 +
      🔊 대본 읽어주기(script_tts) + 📇 제품 프로필 자동 저장. 쿠팡 상품페이지는
      봇 차단으로 미지원 — 블로그 글 경유가 정석)
- [x] 템플릿 모드 — ✅ v0.43 (편집 폼 세팅을 이름으로 저장/불러오기)
- [x] AI 영상 소재 — ✅ v0.45 장면별 AI 이미지 배경 (문장마다 그림 전환, 그림체 6종)
      ※ 영상 생성 AI(시댄스·Veo 등)는 비용(편당 수천~수만 원)·속도 문제로 보류 (토론 결정)
- [ ] 기획안 v1.2 → v1.3 갱신 (PoC 실측 반영)

## 미세 개선 백로그

- [x] 완성 파일명을 제목 기반으로 — ✅ v0.4 (제목.mp4)
- [x] UI [폴더 열기] 버튼 — ✅ v0.4
- [x] Windows 내장 음성 속도 — ✅ v0.44 (설정 → 소리·목소리, SAPI rate)
- [ ] Windows 내장 음성 보이스 선택 (한국어 음성이 여러 개 설치된 PC용)
- [x] BGM 덕킹 기본값 검토 — ✅ v0.44 (기본 켬 + 편집·사진 영상에도 덕킹 구현)
- [x] 긴 문장 자동 분할 안전장치 — ✅ v0.77 (TTS 직전 단어 경계 분할, 강조·장면·재훅 재매핑)
