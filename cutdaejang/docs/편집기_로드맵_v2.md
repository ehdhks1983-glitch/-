# 컷대장 → 완성형 편집기 로드맵 (v2 기획)

- 목표: **캡컷·브루(Vrew) 수준의 완성형 쇼츠·롱폼 편집기**
- 방식: 큰 기능을 작은 단계로 쪼개 **하나씩** 추가 (각 단계는 그 자체로 쓸 수 있게)
- 작성 배경: 사용자 실제 워크플로우 = 직접 촬영한 튜토리얼/리뷰(1~3분) → 자동 자막 → 수정·편집 → 완성

## 현재 상태 (v0.6)

| 기능 | 상태 |
|---|---|
| AI 생성 모드 (주제→대본→TTS→배경→자막) | ✅ |
| 내 영상 편집 (무음컷 + 자동자막 + 세로 쇼츠) | ✅ |
| 상단 제목(훅) 오버레이 | ✅ |
| BGM (AI 모드) | ✅ |
| 자막 **수정** | ❌ ← 지금 가장 아픈 곳 |
| 속도 조절 / 편집모드 BGM / 롱폼 폴리시 | ❌ |

## 관찰된 문제 (사용자 실제 영상 기준)

1. **자막 정확도**: 대체로 맞지만 오인식 존재("랩네이드했나고"). → 고칠 수단 필요
2. **긴 구간 = 한 자막 오래 뜸**: 무음 기준 분할이라 5초 넘게 같은 자막. → 문장 단위 분할 필요
3. **자막 3줄 넘침**: 최대 글자수·줄바꿈 정리 필요

---

## 단계별 로드맵 (우선순위 순)

### ★ Phase 1 — 자막 검토·수정 화면 (P0, 지금 시작)
> "부정확하게 나올 때 편집" 요구 직접 대응. 브루(Vrew)의 핵심 기능.

- [x] 1-1. 자동 자막 생성 후 **바로 렌더하지 않고**, 자막 목록 화면 표시
      (각 줄: 순번 · 시작~끝 시간 · 텍스트, 미리보기 썸네일)
- [x] 1-2. 텍스트 **인라인 수정** (틀린 자막 바로 고치기)
- [x] 1-3. 줄 **합치기 / 나누기(✂ split) / 삭제** (v0.9)
- [x] 1-4. 발음 변환 버튼 재사용(숫자·영어 → 한글)
- [x] 1-5. [이 자막으로 렌더] → 완성. 자막은 job 폴더에 저장(.srt/.json) → 재편집 가능
- [x] 1-6. 특정 구간 **삭제 시 영상도 같이 잘림** = 브루식 텍스트 편집 (v0.41 🗑)

### ✅ v0.9 — 대본 직접 입력 + 검토화면 재생 컨트롤 (사용자 요청, 완료)
> "대본이 미리 있는 경우 넣는 곳", "재생 따라가다 멈출 때", "캡컷·브루 편집 요소".

- [x] 대본 **직접 입력**란 (편집 폼) — 있으면 STT 건너뛰고 영상 타이밍에 배치
      (오인식·비용 0. 줄 수=구간 수면 1:1, 아니면 글자수 비례 분배)
- [x] 검토화면 **영상 플레이어 상단 고정(sticky)** — 스크롤해도 항상 보임
- [x] **스페이스바 = 재생/정지** (브루식 단축키, 입력창 포커스 땐 제외)
- [x] **배속** 0.75~2× (검토 전용), 재생 **시계** 표시
- [x] 줄별 **▶ 그 지점부터 재생**, **✂ 줄 나누기**
- [x] **대본 일괄 붙여넣기** — 대본을 줄 단위로 자막 텍스트에 순서대로 교체

### ✅ v0.10 — 쇼츠 핵심 추출 (긴 영상 → 짧고 굵게, 사용자 요청)
> "무음만 자른 2분짜리는 쇼츠가 아니다. 10~30초로 핵심만." 직접 대응.

- [x] 검토화면에서 자막 줄마다 **넣기 ☑ 체크** → 고른 구간만 남겨 재컷(진짜 쇼츠 길이)
- [x] 선택 길이 실시간 표시 + 완성 버튼이 "선택 구간만 쇼츠로"로 바뀜
- [x] **✨ AI 핵심 추천** — 목표 초를 정하면 Gemini가 임팩트 구간을 골라 체크 (키 없으면 정보밀도 휴리스틱)
- [x] rebuild_from_keep: 고른 자막 구간을 pad+병합해 잘라 이어붙이고 자막 타임라인 재매핑
- [ ] (다음) 여러 개의 짧은 쇼츠로 분할 출력 / 장면 전환 감지 기반 자동 분할

### ✅ v0.11 — 저장 속도 + AI 대본 다듬기 + 훅 강조 (사용자 요청)
> "발음 때문에 대본이 부정확 → 자동 교정", "저장할 때 속도(1.5·2배)", "상단제목 임팩트".

- [x] **저장(렌더) 속도** 1.25/1.5/2배 — 자막 굽고 통째로 배속 → 싱크 유지 (setpts+atempo)
- [x] **🪄 AI로 대본 다듬기** — STT 오인식을 문맥 기반으로 자연스럽게 교정(줄 수·순서 보존)
- [x] **상단 제목(훅) 강조** — `제목 | 단어`로 그 단어만 강조색으로 팝 (썸네일 임팩트)
- [x] 자막 강조도 동일하게 골드 유지 (v0.9.1)

### ✅ v0.12 — 화질 업 (유튜브 노출용, 사용자 요청)
> "유튜브는 화질에 따라 노출이 달라진다 → 화질 올리는 기능."

- [x] 화질 등급 **표준 / 고화질 / 초고화질(4K)** 선택 (검토화면)
- [x] 고화질: CRF↓(저압축) + preset↑ + lanczos 스케일 + unsharp 선명화
- [x] 초고화질: **4K 업스케일**(쇼츠 2160×3840) — 유튜브가 VP9/AV1·고비트레이트로 처리 → 체감 화질↑
- [x] keep 레이아웃은 원본×2 업스케일(최대 3840 캡)
- [ ] (다음) GPU(nvenc) 가속으로 4K 렌더 속도↑

### ✅ v0.13 — 유튜브 썸네일 스타일 배경 띠 (사용자 레퍼런스)
> 실제 인기 쇼츠 썸네일들(굵은 글씨 + 여러 색 + 글자 뒤 어두운 띠)에 맞춤.

- [x] 상단 제목 뒤 **배경 띠**(libass BorderStyle=3 반투명 박스) — 기본 켬
- [x] 자막 뒤 배경 띠 옵션(설정)
- [x] 강조 단어 색(골드/원하는 색) + 띠 조합으로 썸네일 임팩트
- [ ] (다음) 문장 안에서 단어별로 여러 색(노랑+빨강 동시) 지정

### ✅ v0.14 — 대본 맞춤 유튜브 썸네일 자동 생성 (16:9, 사용자 요청)
> "대본을 다 아니 대본에 맞는 최신 유튜브 썸네일을 비율 맞게 만들어줘."

- [x] 완성 영상 프레임(또는 사진) 배경 + 굵은 배경 띠 제목 + 강조색 → 1280×720 PNG
- [x] ✨ AI 카피 추천 — 주제/대본으로 후킹 썸네일 문구·강조단어 여러 개(Gemini, 없으면 템플릿)
- [x] 완료 화면에서 [🖼️ 유튜브 썸네일 만들기] → 미리보기·저장. 텍스트는 영상 자막 엔진 재사용
- [ ] (다음) 문장 내 다색(노랑+초록…) / 배지·스티커(자동 발행 등) / AI 이미지 배경(선택)

### ✅ v0.15 — 제목 자동 강조색 + 영상 잡음 제거 (사용자 요청)
> "썸네일 글씨 색이 자동으로 안 들어감", "영상 잡음이 많은데 처리 방법".

- [x] 제목/썸네일 **숫자 자동 강조** — `| 단어` 없이도 숫자(30개·3가지…)를 강조색으로
      (색이 항상 들어가 임팩트. 명시 `| 단어`는 그대로 우선)
- [x] **🔇 잡음 제거** — highpass + afftdn(FFT 노이즈 리덕션) + lowpass로 배경 잡음·히스 감소
      (목소리 대역 보존). 편집 폼 체크박스, 렌더 시 적용
- [ ] (다음) 문장 내 다색 강조 / 잡음 강도 선택 / arnndn(딥러닝 denoise)

### ✅ v0.16 — 문장 내 다색 강조 (사용자 레퍼런스)
> 인기 썸네일처럼 한 줄에 여러 색(노랑+빨강+초록 동시).

- [x] `[노랑]...[/] [빨강]...[/] [초록]...[/]` 마크업 → 자막·제목·썸네일 부분 색칠
- [x] 색 이름 다수 지원(노랑·빨강·초록·파랑·주황·분홍·보라·하늘·민트·흰…), 닫기 `[/]` 또는 `[/색]`
- [x] 마크업 없으면 기존 `| 단어`·숫자 자동강조로 폴백 (하위호환)

### ✅ v0.24 — 유튜브 쇼츠 안전 영역 정밀 배치 (사용자 요청: 상단 글씨 짤림)
> 쇼츠 UI 구조(상단 헤더 0~220px, 하단 제목·설명 0~420px, 우측 버튼 기둥 ~140px) 기준.

- [x] 상단 제목 여백 0.055→0.125 (1920 기준 106px→240px, 헤더 아래로)
- [x] 자막 하단 여백 비율 0.25 (480px, 제목·채널·음악 영역 위) / top 0.13
- [x] 자막 좌우 여백 110px·제목 90px — 우측 좋아요·댓글 버튼 기둥 회피 (해상도 비례)
- [x] 시각 검증: 위험 영역 오버레이 렌더로 상단·하단·우측 모두 침범 없음 확인

### ✅ v0.25 — AI 내레이션 목소리·자막 싱크 (사용자 리포트: 자막과 내레이션이 안 맞음)
> 원인: 글자 수 비례 '추정' 창에 실제 TTS 클립을 얹기만 해 겹침·밀림·끝 잘림.

- [x] `retime_narration` — TTS 클립 실측 길이로 자막 순차 재배치 (간격 0.12~0.9초 자동 배분)
- [x] 영상보다 길면 말 속도 최대 1.25배 → 그래도 넘치면 뒷문장 생략+안내
- [x] 내레이션 끝나고 영상 꼬리가 1.5초 이상 남으면 자동 트림 (여운 0.7초)
- [x] 렌더 순서 교정: 구간 재컷 → 내레이션 합성 (완전 자동에서 오디오가 옛 타임라인에 남던 버그)
- [x] 완전 자동+내레이션: 목표 초로 대본을 쓰고, 핵심 선별로 대본을 또 자르지 않음
- [x] E2E: 출력 mp4 목소리 구간 vs 자막 구간 Δ≤0.04초 확인

### ✅ v0.26 — 편집 모드 오디오 3종 (사용자 요청)
> "편집에서도 원하는 목소리로", "무료 음원", "원본 소리 무음 옵션".

- [x] 내레이션 **보이스·말투 선택 + 🔊 미리듣기** (생성탭과 동일 성우 목록, 편집 폼)
- [x] **원본 소리** 그대로/작게/무음 — AI 내레이션 입력 시 자동 '무음' (직접 바꾸면 유지)
- [x] **BGM** (Phase 3-1) — resources/bgm 목록/랜덤, 볼륨 3단, 영상 길이 루프 + 페이드 인/아웃
- [x] 오디오 트랙 없는 영상(마이크 없는 화면 녹화)도 무음 트랙으로 렌더 성공 (anullsrc 폴백)
- [x] E2E: 원본 연속 톤 제거(문장 사이 무음 확인) + BGM이 빈틈 채움 + 보이스 파라미터 배선

### ✅ v0.27 — 내 목소리 클로닝 + 완전 자동 배속 (사용자 요청)
> "녹음 파일을 학습해 내 목소리로 내레이션", "쇼츠 만들 때 재생 속도도".

- [x] ElevenLabs 연동 — 녹음 파일(1~3분) → 인스턴트 클론 등록(voice_id 저장, 멀티파트 업로드)
- [x] elevenlabs TTS 제공자 (eleven_multilingual_v2, mp3 응답) — 실패 시 기존 체인 폴백
- [x] 편집 내레이션 보이스에 「🎤 내 목소리」 + 생성탭 목소리에도 라디오 추가, 미리듣기 지원
- [x] 키 인프라 확장 (ELEVENLABS_API_KEY 저장·로드), /api/clone_voice 엔드포인트
- [x] 완전 자동에도 재생 속도(1/1.25/1.5/2배) — 폼에서 선택, E2E 20s→10.07s 확인
- [ ] (참고) 완전 무료 로컬 클로닝(GPT-SoVITS 등)은 GB급 모델+GPU 필요 → 보류

### ✅ v0.28 — GPT-SoVITS 무료 로컬 내 목소리 (사용자 지적: 학습은 한 번만이잖아)
> 유료(ElevenLabs)만 있던 내 목소리에 완전 무료·오프라인 경로 추가.

- [x] GPTSoVITSTTS 제공자 — 로컬 API(api_v2, :9880) 호출. zero-shot: 참조 녹음+대사만
- [x] 설정: sovits_url / sovits_ref_audio / sovits_ref_text + /api/sovits_save (파일 검증)
- [x] UI: 내 목소리 등록을 방법 A(무료·SoVITS)/방법 B(유료·ElevenLabs)로 재구성
- [x] 보이스 「내 목소리(무료·내 PC)」 — 내레이션·생성탭·미리듣기. 서버 꺼짐 시 명확 안내+폴백
- [x] 검증: mock 서버 프로토콜 4종 + E2E(합성 4문장·ko·참조 전달·출력 믹스·폴백)
- [x] 설치 가이드 FAQ Q12 (통합패키지·api_v2·한 번만 학습 팁)

### ✅ v0.28.1 — 무료 BGM 원클릭 받기 (사용자 요청: 음원 미리 채워줘)
> 재배포는 음원 사이트 정책 위반 위험 → 각자 PC에서 합법 다운로드하는 도구로.

- [x] `tools/fetch_bgm.py` + `windows/6_무료음원_받기.bat` — Kevin MacLeod(CC BY) 14곡
- [x] 분위기 태그 파일명(코믹_/브이로그_/미스터리_…) → BGM 목록에서 바로 구분
- [x] 크레딧 파일 자동 생성(설명란 붙여넣기용) + 재실행 시 받은 곡 스킵 + 실패 곡만 재시도
- [x] 검증: 목록 무결성·크레딧 생성·스킵·응답 크기 가드 (mock, 실 다운로드는 사용자 PC)

### ✅ v0.29 — 편집 보이스 키 확보 + BGM 미리듣기 (사용자 리포트)
> "편집은 보이스를 바꿔도 다 똑같다"(원인: 키 미보유 시 내장 음성 폴백을 조용히 수행),
> "BGM 미리듣기 필수".

- [x] 내레이션 미리듣기·편집 시작 시 제미나이 키 프롬프트(ensureGeminiKey, 1회 저장)
- [x] 내장 음성/스텁 폴백으로 보이스가 무시되면 완료 화면까지 남는 경고 표시(tts_warn)
- [x] ▶ BGM 미리듣기 — /bgm/ 서빙(오디오 content-type, 폴더 밖 차단), 편집·생성 양쪽 버튼,
      선택한 볼륨 반영(믹스 느낌), 토글 정지, 랜덤 지원
- [x] E2E: /bgm/ 200·경로탈출 404·키 없는 Puck 선택 → 완료 노트에 "내장 음성" 경고

### ✅ v0.30 — 자막 없는 영상 목표 초 몽타주 + 자막만 모드 (사용자 리포트)
> "30초 설정했는데 4분30초 그대로"(자막 0줄 → keep 선별이 조용히 무시), "목소리 없이 자막만".

- [x] `spread_ranges` — 발화 자막 없는 영상은 전체에서 고르게 조각을 뽑아 목표 길이 몽타주
      (완전 자동+목표 초일 때, 내레이션 유무 무관하게 먼저 컷 → 영상 다양성↑) + 완료 안내
- [x] 「🔇 목소리는 빼고 자막만」 체크 — AI 대본을 하단 자막으로만, TTS 생략, 원본 소리 기본 유지
- [x] E2E: 70초→30.0초 몽타주+안내, 자막만(자막 4줄·TTS 없음·orig keep)

### ✅ v0.31 — 사진 영상 + AI 영상 분석 + 훅 스튜디오 (사용자 요청 3건)
> "사진 5장+15초로 영상", "영상 분석해서 제목·대본", "훅 색 넣기 불편·크기·미리보기".

- [x] photos_to_video — 사진 폴더/파일들+전체 초 → 장당 균등 분배 슬라이드쇼(블러 배경 세로,
      무음 트랙) → 기존 파이프라인(내레이션/자막만/BGM/훅/완전자동) 그대로 결합
- [x] 🧠 /api/analyze_ai — 프레임 4장+자막을 Gemini 멀티모달로 → 제목·훅(클릭 적용)·대본
      (자막 교체 버튼)·해시태그. 키 없으면 스텁 예시+안내
- [x] 🎨 훅 스튜디오 — 드래그 선택+색 칩으로 [색]..[/] 자동, 크기 배수(hook_scale, ass 반영),
      실시간 미리보기(마크업/| 단어/숫자 강조 렌더 규칙 동일)
- [x] E2E: 사진4장→11.9초·전환 확인·자막 4줄·Title 155px(1.4배)·분석 스텁 응답

### ✅ v0.32 — 작업 로그 패널 + TTS 한도 안내 (사용자 리포트)
> "진단 리포트 폴더 찾기 힘듦 — 화면 아래 로그가 낫다", "보이스 제한 걸려 무료 TTS만 적용".

- [x] 🪵 로그 패널 — logging 핸들러 링버퍼(400줄) → /api/state.logs → 하단 접이식 <pre>
      (자동 스크롤·📋 복사), 진단 리포트에도 최근 로그 포함
- [x] 파이프라인 이벤트 로깅(편집 시작·TTS 제공자/폴백·경고·렌더 완료) + tts_engine 폴백 로그
- [x] 한도 인지 경고 — 폴백 사유에 429/quota면 "무료 TTS 한도 — 재실행 시 만든 문장 재사용" 안내
- [x] 버그: 함수 내 늦은 import logging이 모듈 임포트를 가려 UnboundLocalError → 정리
- [x] E2E: 로그 4종 흐름 + 완료 노트 경고 유지

### ✅ v0.33 — 쇼츠 핵심 선별 강화 (사용자 리포트: 1초~30초가 그대로 나옴)
> 원인: Gemini 프롬프트에 연속 선택 금지 없음 + 휴리스틱 동점 시 인덱스순(앞부터).

- [x] 프롬프트 강화 — 각 줄에 [시작시각] 제공, "앞에서부터 채우기 금지·앞중뒤 모두 훑기" 명시
- [x] 게으른 선택 가드 — 응답이 0부터 연속이거나 앞 40%에 몰림(영상이 목표 1.6배 이상일 때)
      → 무효 처리 후 후킹 점수 방식으로 폴백
- [x] 휴리스틱 3등분 버킷 라운드로빈 — 동점이어도 앞·중간·뒤를 모두 커버
- [x] 투명성 — 고른 구간 타임스탬프를 완료 화면·로그에 표시 (✂️ 핵심 N구간: 0:03 · …)
- [x] 검증: 단위 4종(커버리지·접두 무효·분산 통과·짧은 영상 허용) + 엔드포인트 E2E

### ✅ v0.33.1 — 전체 코드 점검 (사용자 요청) — 결함 15건 수정
> 정적 분석(pyflakes) + 핫패스 정독 + 병렬 모듈 점검 2회로 전 모듈 검토.

- [x] [높음] 훅 스튜디오 미리보기·지우기 무동작 — JS `[]]` 빈 클래스 정규식 4곳 (브라우저 E2E 검증)
- [x] [높음] 무오디오 영상(마이크 없는 녹화) 컷·몽타주에서 하드 크래시 — cut_and_concat 오디오 조건부
- [x] [높음] SoVITS 내 목소리 캐시 오염 — 참조 녹음을 캐시 키에 반영(기존 캐시 보존)
- [x] [중] mp4 렌더 예외가 CapCut draft 생성까지 막음 — A/B 독립 try 분리
- [x] [중] Gemini STT 빈 응답이 영구 빈 자막으로 캐시 고착 — 오류 전파 + 빈 결과 캐시 금지
- [x] [중] settings.json 오타 시 조용히 기본값 — 경고 로그 + 저장 원자화(tmp+replace)
- [x] [중] photos_to_video가 요청 길이 초과(장당 0.5초 바닥) — 고르게 추려 길이 준수 + 경고
- [x] [중] 사진 폴더 사전식 정렬(1,10,2…) — 자연 정렬 / 사진 모드 몽타주 중복 컷 제외
- [x] [하] CLI 대본 txt cp949 폴백 · probe "N/A" 가드 · 진행률 out_time_ms 폴백+콜백 보호+
      -nostdin · 렌더 재시도 성공 시 유령 오류 제거 · run_topic 예외→실패 결과 ·
      draft 색상 폴백 · STT 바운더리 uuid · TTS raw 동시 잡 경쟁 방지 · 구간 60+ 필터 스크립트 파일
- [x] 정리: 죽은 임포트 8건 (pyflakes 잔여 0)
- [ ] (기록) 낮음·설계 취약으로 보류: JobStore 스레드 설계, Whisper 클래스 캐시 동시성,
      spec 짝수 해상도 검증 — 단일 사용자 로컬 앱에선 실위험 낮음

### ✅ v0.34 — 자막 2줄 제한(자동 분할) + 폼 초기화 (사용자 요청)
> "9:16인데 자막이 4줄 — 2줄 이상 안 넘는 게 좋겠다", "초기화 버튼이 없다".

- [x] split_long_subtitles — 2줄(wrap×2자) 초과 자막을 단어 경계로 쪼개 여러 자막으로
      (시간 글자 수 비례 배분·내용 무손실·강조어는 해당 조각에 보존, 공백 없는 초장문 폴백)
- [x] 적용 지점: 분석 직후(STT·대본 입력) + 렌더 직전(검토 수정분, 내레이션은 문장=클립 유지)
      → 4줄의 원인이던 '2줄로 접어도 폭 초과 → libass 재접기' 원천 차단
- [x] ↺ 초기화 — 편집 폼 전체(영상·사진·훅·내레이션·오디오·완전자동…)와 생성 폼, confirm 후
- [x] 검증: 단위 2종(분할 무손실·무공백 폴백) + 브라우저 E2E(초기화 전 필드 복원)

### ✅ v0.35 — 워터마크 + 4K 화질 강화 + 훅 색 UX (사용자 요청 3건)
> "워터마크 삽입 + 편한 UI", "4K 화질업이 제대로 안 됨", "색 적용 불편".

- [x] 🏷️ 워터마크 (Phase 3-3) — 편집 폼 한 줄 UI(경로·위치 4곳·크기 3단), 투명 PNG,
      쇼츠 안전영역 오프셋(상단 13.5%·하단 23.5%), 자막 아래 오버레이, settings에 기억,
      없는 파일은 경고 후 생략. 입력 인덱스(내레이션·BGM 동시 사용) 동기 검증
- [x] 🖼️ 4K: ultra crf 19→16 + CAS 적응형 선명화(cas=0.55, 헤일로 없는 업스케일 보정)
- [x] 🎨 훅 색: 커서 단어 자동 감싸기(드래그 불필요)·같은 색 토글 해제·다른 색 교체·
      기존 태그 정리(중첩 방지)
- [x] 검증: 워터마크 4꼭짓점+조합 스모크·API E2E(YAVG 189 vs 배경 16, 설정 저장),
      훅 UX 브라우저 E2E 3케이스, 전체 203개 테스트

### ✅ v0.36 — 초보자 UI 전면 개편 (테스터 피드백 "사용하기 어렵다")
> 기능은 그대로, 화면 구조만 초보자 기준으로 재배치.

- [x] 🏠 첫 화면 = 모드 카드 3개(🤖 AI 영상 / ✂️ 내 영상 편집 / 📸 사진으로 영상)
      — 카드 클릭 → 해당 폼만 표시, [← 처음으로] 복귀. 사진 모드가 전용 카드로 독립
      (편집 폼 내 숨은 박스 제거, video/photo 블록 전환 + 사진 모드는 세부 설정 숨김)
- [x] 🔢 단계형 폼 — ① 영상/사진/주제 ② 완성 방식(생성 폼은 목소리) ③ 꾸미기
- [x] 🗂️ 꾸미기 = 접힌 그룹(details.opt) 6개: 훅 / AI 내레이션(+내 목소리) /
      소리·BGM·잡음 / 워터마크 / 대본 직접 / 세부 설정(비율·자막·무음컷·STT·키)
- [x] 🤖 완전 자동: 체크박스 → "어떻게 완성할까요?" 라디오(검토 추천/자동) +
      완성 길이 프리셋(30초/60초/원본/직접) — auto_edit·auto_target_sec 파라미터 동일
- [x] 진행 UX: 시작하면 폼 숨기고 진행 카드 집중, 완료 화면 🎉 + [🏠 처음으로],
      검토 화면 ①②③ 안내 + 보조 도구는 [🧰 더 많은 도구]로 접기
- [x] 💡 처음 사용자 안내(Gemini 키 없으면 첫 화면 가이드), 상단 ⚙ 설정 버튼
- [x] 잔재 제거: appmode 라디오·autoEditChk·switchAppMode → openMode/showHome/
      onFinishChange/applyTargetPreset. BGM 힌트의 \6 octal-escape 표시 버그 수정
- [x] 검증: 브라우저 E2E 39체크(홈→AI 생성 완주→편집 검토→완성 완주→사진 모드→
      초기화, JS 오류 0) + UI 구조 유닛 테스트 + 전체 204개 테스트

### ✅ v0.37 — 내 목소리(클로닝) 전면 배치 (사용자: "업데이트가 안 된 거 같아" = 못 찾음)
> 기능(v0.27 ElevenLabs·v0.28 SoVITS)은 있었지만 편집 폼→꾸미기→내레이션→접힌 줄
> 3단계 중첩이라 발견 불가. 전용 화면으로 독립.

- [x] 🎤 voiceCard 전용 화면 — 방법 A(GPT-SoVITS 무료)·방법 B(ElevenLabs) 등록 폼,
      방법별 [🔊 미리듣기](previewMyVoice: sovits는 참조 저장 후, elevenlabs는 키 가드),
      등록 후 사용법 안내 박스, 본인 목소리 경고
- [x] 진입 3경로 + 원위치 복귀 — 홈 버튼 / AI 생성 ② 목소리 바로가기 / 내레이션 그룹
      바로가기 (openVoice가 _voiceReturn 기억 → closeVoice가 그 화면으로)
- [x] ✅ 등록 상태 배지 3곳(markMyVoice): 홈·전용 화면·내레이션 그룹
- [x] 검증: 브라우저 E2E 22체크(진입/복귀 3경로, 미등록 가드, SoVITS 등록→상태·보이스
      목록·생성 폼 라디오, 모의 9880 서버 미리듣기 실호출, settings 저장) + 구조
      유닛 테스트(입력 id 중복 금지 포함) + 전체 205개 테스트

### ✅ v0.38 — 완전 자동 세팅 기억 + 여러 쇼츠 나누기 (사용자 요청 2건)
> "세팅 저장 → 다음부터 자동 적용", "10분 영상을 쇼츠 7~10개로".

- [x] 💾 세팅 기억: /api/edit가 _EDIT_LAST_KEYS(방식·길이·속도·화질·multi·소리·BGM·
      wm·보이스·STT 등 20키, 경로·주제·대본·키 제외)를 settings.ui.edit_last로 저장 →
      poll 최초 로드 시 applyEditLast()가 폼 복원(내 목소리 옵션 추가 후, STT는 loadStt에서).
      [↺ 초기화]는 기본값 스냅샷을 덮어써 기억도 리셋
- [x] 🎬 auto_multi: ② 완성 방식에 만들기(1개/여러 개) — _run_edit 자동 분기에서
      _do_edit_split 호출(내레이션·사진과 배타, 몽타주 사전 컷 생략). _do_edit_split는
      자막 없으면 시간 균등 분할 폴백(1.5×tgt 미만 거부, 짧은 꼬리 병합, 상한 30개) +
      edit_params의 orig_audio/bgm/watermark/hook_scale 각 쇼츠 적용
- [x] 🎞️ 완전 자동 화질 select(autoQualitySel) — 기존 quality 파라미터에 연결
- [x] 검증: 브라우저 E2E 22체크(여러 개 완주 edited_1..N, 새로고침 복원 7항목,
      초기화 리셋, 길이 0 가드, 0→60 교정) + 유닛(분할·세팅 저장·노출) 206개 테스트

### ✅ v0.39 — 📦 유튜브 업로드 키트 (사용자 제안, 토론 후 방향 확정)
> "만든 영상을 버튼 하나로 제목·태그·설명·키워드·카테고리까지". 확정 방향: 유튜브 먼저 ·
> 완료 화면+히스토리 진입 · 채널 정보 맞춤 · 문구 생성만(자동 업로드는 추후 검토).

- [x] suggest_upload_kit(+stub) — 프레임 4장+대본+훅+채널 정보 멀티모달 →
      titles/description/tags/keywords(10)/hashtags(3)/category(고정 14종 검증)+이유
- [x] /api/upload_kit — 대본 수집(메모리 자막→script.json), probe로 쇼츠 판별,
      BGM CC BY 크레딧 자동 삽입(_bgm_credit: fetch_bgm TRACKS 매칭), 체크리스트 계산,
      업로드킷.txt 저장. 히스토리 영상(서버 재시작 후)도 지원
- [x] UI — 완료 화면 [📦] 버튼+kitBox(제목 클릭=복사·설명/태그 복사·다시 만들기),
      히스토리 행 [📦 업로드 키트], ⚙ 설정 「내 채널 정보」(name/topic/audience)
- [x] _do_edit_render가 최종 자막을 job에 저장 → 완전 자동 작업도 대본 활용
- [x] 검증: 브라우저 E2E 17체크(채널 저장·키트 표시 8항목·파일 생성·제목 복사) +
      유닛(스텁 구성·파일·400) 208개 테스트
- [ ] (다음 후보) 인스타 릴스·틱톡·네이버 블로그 문구 / 유튜브 API 자동 업로드

### ✅ v0.40 — AI 배경 투명화 + 대본·배경을 목소리에서 분리 (사용자 질문·피드백)
> "AI 배경 항상 켜는데 적용되는 게 맞나?"(확인 불가) + "하면 할수록 복잡해진다".

- [x] prepare_background → (path, source) 반환(user/ai/ai_fail:사유/local),
      JobResult.bg_source → 완료 화면 「🖼️ 배경: AI 이미지 ✨ / 기본 그라데이션(사유)」+로그
- [x] 게이팅 단순화(_ai_image_setup): script_provider 조건 제거 — 키만 있으면 목소리와
      무관하게 AI 배경(테스트 톤만 제외), 미사용 사유를 그대로 표시
- [x] 대본도 분리: generate()가 테스트 톤 외엔 항상 gemini 대본 요청, 서버가 키 없으면
      템플릿으로 안전 강등+안내 (내장 음성 사용자도 키만 있으면 진짜 AI 대본)
- [x] ⚙ 설정 카드 4그룹 접기(자막·제목/배경·모션/소리·목소리/채널) + AI 배경 안내 개선
- [x] 검증: 출처 3종·게이팅 매트릭스 유닛 + 완료 배지 브라우저 확인 + 전체 210개 테스트

### ✅ v0.40.1 — 배경 띠 이음새 제거 (설정 비교 이미지 만들다 발견 → 사용자 "고쳐줘")
> 색 강조({\1c})가 있는 줄은 BorderStyle=3 박스가 색 구간(run)마다 그려져
> 반투명 박스가 경계에서 겹침 → 세로 줄무늬.

- [x] band_event_lines: 띠는 색 태그 제거+글자 투명(\1a&HFF&) '유령 이벤트'(단일 run
      → 박스 한 장, layer 0), 글자는 {\bord0}으로 박스 없이 layer 1 — 제목·자막 공통
- [x] 색 없는 줄·띠 꺼짐은 기존 1이벤트 유지(이중 그리기 방지), 페이드는 유령에도 부착
- [x] 검증: 띠 상단 스트립 열별 밝기 실측 퍼짐 45→1.4 + 구조 유닛 5케이스 + 전체 211 테스트

### ✅ v0.41 — 캡컷 제거 + 문장 타임스탬프 + 트림·브루식 편집 (사용자 지시 3건)

- [x] 캡컷(draft) 전면 제거 — draft_builder·UI 체크박스·CLI 옵션·설치 bat·pycapcut 의존·
      문서. JobOptions/JobResult 필드 정리 (히스토리 DB 스키마는 호환 유지)
- [x] 문장 타임스탬프: FasterWhisperSTT.transcribe_timed + STTEngine.transcribe_timed
      (seg.json 캐시·환각 조각 필터) + transcribe_segments_timed/build_subtitles_timed
      (상대→절대 시각, 클램프, 0.2초 미만 제거). 미지원 제공자(gemini)는 구간=1줄 폴백
- [x] ✂️ 앞뒤 트림: 검토 화면 ⏮/⏭(재생 위치 기준) + 범위 밖 줄 자동 해제·복구,
      서버 _apply_trim(영상 컷·자막 시프트·keep 재매핑) — 렌더·분할 양쪽 적용
- [x] 🗑 브루식 삭제: 줄 버튼 → 자막+영상 구간 통째 제외 (기존 keep 컷 기제 재사용)
- [x] 완료 화면에 안내(note) 표시 — 트림·핵심 구간·TTS 경고가 완료 후 안 보이던 문제 수정
- [x] 검증: 유닛 6종(타임드 캐시·필터·매핑·analyze 6줄·트림 API 길이 실측) +
      브라우저 E2E 15체크(트림 라벨·자동 해제·🗑·조합 길이 4.0s 실측) + 전체 216 테스트

### ✅ v0.42 — 내레이션 끝까지 담기 + 배치 모드 (사용자 선택 4건 중 ①②)

- [x] ⏸/🔁 내레이션 fit: retime_narration(fit=) — freeze/loop면 속도·생략 없이
      350ms 간격 순차 배치(넘침 허용), video_editor.extend_video()로 영상을
      tpad(마지막 프레임 정지) 또는 -stream_loop(반복)로 내레이션 끝까지 연장.
      기본값 freeze(추천), drop = 예전 방식(속도↑+뒷문장 생략). 완료 화면에
      "⏱ 내레이션이 길어 정지/반복으로 N초 연장" 안내. narr_fit는 세팅 기억에 포함
- [x] 📦 배치 모드: 생성 폼 1단계 「여러 개 한 번에(배치)」 체크 → 여러 줄 주제
      (최대 20개) → /api/generate_batch — 주제마다 대본 생성→run_job을 순차 실행
      (진행률 k/N 표시), 영상별 히스토리 기록, 마스터 잡에 mp4 목록+
      "📦 배치 완성: N/M개"(부분 실패 시 오류 요약). 검토 없이 auto 강제
- [x] 검증: 유닛/API 4종(freeze 연장 실측 9.5s→내레이션 끝까지·drop 길이 유지·
      배치 2주제 mp4 2개+히스토리·빈 주제 400) + 브라우저 E2E 13체크
      (narrFitSel 기본 freeze·배치 토글·빈 주제 알림·2개 실주행 2/2 완성) + 전체 220 테스트

### ✅ v0.43 — 꾸미기 4종: 전환·자막 팝·템플릿·인트로/아웃트로 (사용자 선택 ③)

- [x] 🎬 전환(fade): cut_and_concat/photos_to_video/rebuild_from_keep에 transition= —
      구간 안 fade in/out(0.14s, 사진 0.3s)이라 길이 불변·자막 싱크 유지, 소리 무변경.
      적용: 사진 슬라이드·몽타주·핵심 재컷·분할 쇼츠 (무음 컷 경계는 제외 — 말 흐름).
      UI: optFx 그룹(편집·사진 양쪽 표시), edit_last·템플릿에 포함
- [x] ✨ 자막 팝: Style.anim("none"|"pop") → dialogue_text에 {\fscx86\t(0,130,\fscx100)}.
      band+팝이면 2층(유령 띠는 태그 제거로 고정, 글자만 팝 — 박스 요동·이음새 방지).
      설정: subtitle.anim + 설정 카드 select
- [x] 📋 템플릿: settings.ui.templates{이름: _EDIT_LAST_KEYS 스냅샷} ≤20개.
      /api/template(save/delete — save_settings_replace로 진짜 삭제), 편집 카드 상단 바
      (선택=applyEditLast 재사용, 💾 프롬프트 저장, 🗑 삭제), 새로고침 후 state로 복원
- [x] 🎞 인트로/아웃트로: settings.branding{intro,outro} + 설정 카드 입력.
      video_editor.attach_branding() — 사진은 2.5s 정지 클립, 해상도 자동 맞춤(pad),
      무음 클립은 무음 트랙 생성 후 concat. 모든 완성 경로에 적용(_attach_branding:
      편집 단일·분할 각각·gen 단일·배치 각각) — 실패해도 본편은 살리고 안내만
- [x] 검증: 유닛 7종(길이 불변+휘도 dip 실측·사진 전환·브랜딩 길이/해상도/오디오·
      스킵 케이스·팝 2층 ASS) + API 2종(템플릿 저장 필터/삭제·사진 fade+인트로 실측
      6.5s) + 브라우저 E2E 14체크(템플릿 저장→적용→삭제→새로고침 유지·사진 모드
      optFx·설정 저장 반영) + 전체 227 테스트

### ✅ v0.44 — 마무리 3종: 장면 스냅·내장 음성 속도·덕킹 기본 켬 (사용자 선택 ④)

- [x] 🎬 장면 전환 감지: detect_scene_changes(select scene+metadata=print 파싱, μs) +
      shift_ranges_to_scenes(몽타주 — 구간 통째 이동, 길이 보존, 겹침 방지) +
      snap_boundaries_to_scenes(분할 — 내부 경계만, 최소 3초 보장). 자동 적용:
      목표초 몽타주·시간 기준 분할 (20분 미만 영상, 실패 시 스냅 없이 진행)
- [x] 🗣 내장 음성 속도: tts.windows_rate(-10~10, 클램프) → WindowsTTS(rate=) →
      SAPI $s.Rate 치환. 캐시 키에 rate 반영(0은 기존 캐시 유지). 설정 카드 select
- [x] 🎚 덕킹: DEFAULTS bgm.duck=True + 편집 모드에도 구현 — render_edited(bgm_duck=)
      목소리(원본+내레이션) 선합성 → sidechaincompress → BGM 합성. gen은 기존
      composer 경로가 기본 켬으로 활성화. 단일·분할 렌더 모두 settings에서 전달
- [x] 검증: 유닛 5종(장면 감지 2s/4s 실측·스냅 순수 로직 2종·rate 클램프/캐시 키·
      덕킹 렌더) + 브라우저 E2E 9체크(설정 UI·저장·24초 2장면 분할이 12/12 대신
      11/13로 스냅되는 실측) + 전체 232 테스트
      ※ 합성 순색(빨강→초록)은 ffmpeg 장면 메트릭 점수가 낮아 테스트 소재를
      휘도 차 색으로 사용 (실제 촬영 영상과는 무관한 특이 케이스)

### ✅ v0.45 — 장면별 AI 이미지 배경 (사용자 토론 확정: 이미지 방식·문장당 1장·스타일 UI)

> 영상 생성 AI(시댄스·Veo — 편당 수천~수만 원)는 비용·시간·통제 문제로 보류하고,
> "문장마다 AI 그림이 넘어가는" 이미지 방식으로 결정 (편당 0~500원, 무료 한도 내 0원).

- [x] 대본: PROMPT에 문장별 scene(장면 묘사, 글자 금지) 추가 — Script.scene_prompts
      (병렬 정규화·JSON 왕복·구버전 호환), 스텁도 장면 4개 생성
- [x] 이미지: background_generator.IMAGE_STYLES 6종(일러스트·실사풍·3D·수채화·네온·
      미니멀) + generate_scene_images(스타일 접두, 한 장 실패는 None) +
      fill_scene_gaps(직전/다음/기본 배경으로 채움)
- [x] 합성: scene_slideshow — 문장 오디오 클립 타이밍 그대로 [(이미지, 구간)] →
      장면별 켄번즈 줌(-framerate 명시로 25fps 어긋남 버그 잡음) + 경계 페이드 +
      concat → bg_slides.mp4. Background type="video" 신설(spec 검증·missing_files·
      composer 입력 + tpad/trim 안전 정규화, regenerate 호환)
- [x] 오케스트레이터: build_spec 후 스팬 계산 → 슬라이드 배경 교체.
      성공 2장 미만/⅓ 미만이면 단일 배경 유지. bg_source="ai_scenes:N/M" →
      완료 화면 「AI 장면 이미지 N/M장 ✨」
- [x] UI: 생성 폼 「🖼 AI 배경 그림」(그림체 6종, 생성 시 settings.bg.image_style로
      기억·복원) + 설정 「문장마다 새 이미지」 토글. bg.ai_image 기본값 True로
      (키 없으면 자동 생략 — 기존 저장 설정은 유지됨)
- [x] 검증: 스모크 18체크(색 순서·길이·최종 영상 장면 전환 픽셀 실측) → 정식 유닛
      7종 + API 1종(bg_style 기억) + 브라우저 E2E 11체크 + 전체 240 테스트

### ✅ v0.46 — ElevenLabs 성우 보이스 (사용자 요청: 계정 보이스 자동 불러오기)

> 클로닝(v0.27)만 있던 ElevenLabs 연동을 기성 성우 보이스 선택으로 확장.
> 비용: 글자 과금 — 60초 쇼츠 ≈ 300자, Starter $5/월 = 30,000자 ≈ 100편(편당 ~70원).

- [x] tts_engine.list_elevenlabs_voices() — GET /v1/voices, 클론 먼저·이름순.
      ElevenLabsTTS.synthesize는 명시 voice_id 그대로 사용 (기존 구조 재사용)
- [x] /api/eleven_voices — 키 없으면 no_key, 10분 프로세스 캐시
- [x] 생성 폼: 「🎙 일레븐랩스 성우」 라디오(키 있을 때만) + elevenVoiceSel
      (내 계정 보이스, 클론 표시) + 🔊 미리듣기 + 비용 안내.
      generate()는 eleven_voice → tts_provider=elevenlabs + voice_id 매핑
- [x] 내레이션: narrVoiceSel에 el:<voice_id> 항목 자동 추가 — 서버 체인
      elevenlabs 우선 + 폴백 경고, 미리듣기 el: 매핑, edit_last 늦은 복원 버퍼
- [x] 검증: 유닛 2종(파싱 정렬·no_key) + 브라우저 E2E 8체크(라디오 노출·목록·
      내레이션 el: 3개·라벨) + 전체 242 테스트

### ✅ v0.47 — 업로드 키트 5플랫폼 (사용자 요청: 제목 태그 + 틱톡·인스타·클립·스레드)

> 웹 조사로 2026 권장 확인: 유튜브 제목 40~60자·해시태그 3~5(15개 초과 시 전부
> 무시)·설명 첫 125자 노출 / 틱톡 태그 3~5(fyp류 무효) / 인스타 3~5 / 네이버 클립
> 태그 10~12 / 스레드 500자·토픽 태그 1개만.

- [x] 프롬프트 확장: 한 번의 생성으로 유튜브(titles+title_tags 2~3개)+틱톡+인스타+
      네이버클립+스레드. normalize_kit()로 방어 정규화(＃ 제거·개수·길이 제한·
      누락 플랫폼 빈 구조 보장), 스텁도 동일 구조
- [x] UI: 제목 버튼 = 「제목 #태그 #태그」 통째 복사, 플랫폼별 접이식 섹션 4개
      +📋 복사(summary 안 버튼 접힘 방지). 업로드킷.txt에 【틱톡】…【스레드】 섹션
- [x] 잠복 버그 2건: 완전 자동 생성 영상의 키트가 script.json(dict 문장) join에서
      서버 크래시 → 타입 안전 파싱. do_POST 전역 가드 신설 — 어떤 핸들러 예외도
      'Failed to fetch' 대신 JSON 오류+로그로
- [x] 검증: 유닛 2종(정규화·스텁) + API 2종(편집·완전자동 생성 키트) + 브라우저
      E2E 11체크(제목+태그 복사 실측·플랫폼 4칸·접힘 방지) + 전체 247 테스트

### ✅ v0.50 — 마스코트 캐릭터 + 장면 그림 검토 (사용자 요청: 뼈다귀 캐릭터·프롬프트 확인 방식)

> "요즘 잘 되는 해골 캐릭터 경제 쇼츠처럼" + "이미지 만들기 전에 프롬프트가 쭉
> 나오고 확인 후 넣는 방식" — AskUserQuestion으로 프리셋+직접묘사 / 검토화면 확정.

- [x] 캐릭터 주입: CHARACTER_PRESETS 5종(해골·고양이·곰돌이·직장인·스틱맨)+직접 묘사,
      scene_prompt_text가 "주인공: … 모든 장면에 같은 모습·같은 그림체" 문구로 합성
- [x] 일관성: 첫 성공 이미지를 ref_png로 이후 장면 생성에 첨부(Gemini inlineData) —
      같은 캐릭터·같은 그림체 유지. 재생성 때도 다른 성공 장면을 참조로
- [x] 검토 흐름: 대본 확정 → _prepare_scenes(그림 먼저 생성) → review_scenes 상태
      → 그리드(그림+문장+프롬프트 수정+🔄 다시 그리기) → ✅ 확정 → 확정본 그대로
      렌더(run_job scene_images 전달 — 재생성 없음). 전부 실패 시 기존 흐름 폴백
- [x] UI: 「마스코트」 셀렉트(+직접 쓰기 입력), 설정 bg.character 기억·복원·초기화
- [x] 검증: 유닛 3종(프롬프트 주입·ref 체인·사전 이미지 렌더) + 서버 왕복 1종
      + 브라우저 E2E 19체크(그리드 4장·재생성 실동작·완성 배지) + 전체 255 테스트
- [x] v0.50.1 핫픽스(사용자 리포트 "설정에서 AI 배경 꺼짐"): 진짜 원인 = zip에
      체크인된 옛 settings.json(ai_image:false+은퇴 모델명)이 업데이트마다 사용자
      설정을 덮어씀 → ① settings.json 저장소에서 제거(.gitignore, 업데이트 안전)
      ② migrate_settings 1회 승격(cfg_v 스탬프 — ai_image 복구+모델명 교체, 이후
      사용자가 직접 끈 건 유지) ③ 폼에 꺼짐 경고+[지금 켜기] ④ [⬇ 무료 BGM 받기]
      버튼(/api/fetch_bgm 백그라운드+진행 표시 — bat과 동일 동작) ⑤ 조사 수정
- [x] v0.50.2(실사용 로그): ① TTS 429 중 일일 한도(quotaId PerDay)는 재시도 없이
      즉시 TTSExhausted → 폴백 (문장마다 120초 낭비 제거, 리셋 시각 안내)
      ② Gemini STT thinkingBudget 0 — 2.5-flash 씽킹이 짧은 받아쓰기를 삼켜
      '빈 응답' 경고 8건 나던 것. 출력 없는 STOP은 무음 처리(경고 X), candidates
      없음(차단·쿼터)만 오류 유지 ③ 이미지 생성이 텍스트로 응답하면 "이미지
      1장만" 지시를 붙여 1회 재시도 (장면 1장 이웃 대체되던 케이스)

### ✅ v0.51 — 이미지 비용 관리 (사용자 요청: 크레딧 소진 + 장수 설정 + 수동 삽입)

> "이미지 장수를 내가 설정" + "프롬프트가 쭉 나오고 복사해서 챗지피티/제미나이에서
> 한 번에 만들고 수동으로 넣는" 흐름 — 컷대장 쪽 이미지 비용 0원 경로 신설.

- [x] bg.scene_mode(auto/manual/off) + max_scene_images: select_scene_indices 균등
      배치, only_indices로 선택 문장만 생성, 사이 문장은 fill_scene_gaps 직전 그림,
      merge_scene_spans로 같은 그림 연속 구간 줌 이어붙임 (툭 끊김 방지)
- [x] ✍ manual: 키 없이도 검토 진입(_prepare_scenes 생성 생략) → [📋 프롬프트 전체
      복사](지시문+번호, 그림체·마스코트 포함) → /api/scene_folder(이름순 일괄)·
      /api/scene_upload(장면당 1장, normalize_to_canvas 정규화) → 확정 렌더.
      전부 실패한 auto도 이제 검토로 넘어와 직접 채울 수 있음
- [x] 429 크레딧 소진(prepayment credits): TTS is_daily_quota 확장(즉시 폴백),
      이미지 생성 루프 중단(_is_quota_error — 남은 장면 호출 안 함), do_POST 가드
      친절 안내(원문 JSON 노출 제거), scene_regen/confirm_scenes 문장 인덱스 매핑
- [x] pick_path(video/image/audio/folder) 일반화 + 사진 폴더 [📁] 버튼 + 일레븐
      라디오 상시 노출(키 없으면 등록 화면 안내)
- [x] 검증: 유닛 3종(균등·병합·한도 중단·장수 제한 run_job) + 서버 왕복 2종(수동
      흐름·픽커 라우팅) + 브라우저 E2E 17체크 + 전체 265 테스트

### ✅ v0.52 — 상단 제목 스튜디오 + 내 목소리 등록 UX (사용자 확답 반영)

- [x] 순서 교체(사용자: "위아래 변경"): AI 제목 추천(키워드 입력+후보)이 위,
      제목 확정 입력이 아래 — 생성·편집 폼 공통
- [x] 글씨 스타일 프리셋 4종: ass_writer.HOOK_STYLES(기본/예능 노랑/화이트 박스/
      네온) — Title 스타일 색·테두리·띠 교체, 네온은 \\blur 글로우, 강조 복원색도
      프리셋 기본색으로. spec.Style.hook_style + subtitle.hook_style 설정 기억,
      edit_params로 편집·분할 렌더에도 적용, 화면 미리보기(HOOK_STYLE_CSS 근사)
- [x] 색 넣기 개선(사용자: "지금 방식 불편"): 드래그 선택 지원 명시 + 생성 폼에도
      색 칩·미리보기 (전엔 편집 폼 전용). suggestHooks 버튼 내 span 클릭 안전화
- [x] 내 목소리 등록(사용자 확답 ㉯): 녹음 파일 [📁] 픽커(audio) 2곳 + 가입→키
      발급→등록 단계별 안내·바로가기 링크(api-keys/voice-library)
- [x] 검증: ass 유닛 1종(프리셋 4종 색·테두리·blur·복원색) + 서버 2종(순서·픽커
      구조, hook_style 기억) + 브라우저 E2E 14체크(순서·칩·미리보기 색 변화·
      픽커·렌더 상단 노란 픽셀 실측) + 전체 268 테스트

### ✅ v0.53 — 효과음 자동 (편집자급 ①, 사용자 일괄 승인 "순차적으로 쭉")

- [x] core/sfx.py: ffmpeg 합성 3종(뿅=상승 처프+감쇠, 휙=분홍잡음 대역+볼록 볼륨,
      띠링=배음 2개+감쇠) → resources/sfx 캐시, 같은 이름 사용자 파일(mp3 등) 우선
- [x] 배치: spec 기반 — 훅 있으면 띠링 0.15s, 강조(하이라이트/색 마크업) 문장 시작에
      뿅(첫 문장 제외, 최대 6), 장면 전환 0.12s 전 휙(최대 12, -3dB). spec.Sfx 직렬화
- [x] 믹스: render()에서 보이스 트랙에 amix(duration=first·normalize=0) — 길이·
      볼륨 유지, 실패 시 원본 복사(렌더는 계속)
- [x] 설정 sfx.enabled/volume_db + 생성 폼 「🎵 배경음악」 체크(기억·복원·초기화)
- [x] 검증: 유닛 4종(합성·사용자 우선·배치 규칙·무음 위 믹스 dB 실측) + 통합(스텁
      run_job spec.sfx + voice_sfx.m4a 사용) + 서버 1종(켬/끔 기억) + E2E 6체크
      + 전체 273 테스트

### ✅ v0.54 — 디자인 자막 프리셋 (편집자급 ②)

- [x] ass_writer.SUB_STYLES 4종(기본/예능 노랑/말풍선 띠/네온) — Default 스타일의
      색·테두리·그림자·띠(색 포함)를 dataclasses.replace로 덮고, 강조·복원색도
      프리셋 기본색을 따름. 네온은 자막 이벤트에 \blur 글로우
- [x] Style.sub_style + subtitle.sub_style 설정, 생성 폼·편집 폼(세부 설정) 셀렉트
      — 기억·복원·초기화, edit_params로 편집·분할 렌더에도 적용
- [x] 검증: ass 유닛 1종(4종 색·띠 강제·blur·강조 복원색) + 서버 1종(셀렉트 2곳·
      기억·spec 반영) + E2E 6체크(예능 노랑 실렌더 — 자막 영역 노란 픽셀 실측)
      + 전체 275 테스트

### ✅ v0.55 — 펀치인 줌 (편집자급 ③)

- [x] spec.Punch/punchins + 오케스트레이터 배치(강조 문장 시작~1.2s, 최대 8 —
      효과음 '뿅'과 같은 규칙이라 소리·화면 동기)
- [x] composer: 자막 굽기 직전 zoompan(z=punch_zoom_expr — on/fps 프레임 기반
      between, 램프 인 0.012/f·아웃 0.008/f, 1.10 상한) → 자막·제목은 고정
- [x] 설정 bg.punch_in 기본 켬 + 생성 폼 「자막 글씨 스타일」 그룹 체크(기억·복원)
- [x] 검증: 유닛 3종(z식·직렬화·명령 포함/미포함 + 실렌더 남색 사각형 면적 1.12배↑
      실측 + run_job 배치/끔) + 서버 1종 + E2E 5체크 + 전체 279 테스트

### ✅ v0.56 — 숫자 인포 팝 + 화면 톤 (편집자급 ④ — 4종 세트 완성)

- [x] 톤 프리셋 5종(composer.TONE_PRESETS: 기본/시네마틱/화사/선명/흑백) —
      시각 체인에서 자막 굽기 전 적용(글자 원색 유지). Style.tone ← bg.tone,
      편집 모드도 subs_arg 접두로 동일 적용(edit_params tone)
- [x] 숫자 인포 팝: 강조 문장 속 숫자 정규식 추출(최대 4) → spec.InfoPop →
      ass Info 스타일(0.085h, 강조색, an5 중상단)에 \t 팝 + \fad 이벤트
- [x] UI: 톤 셀렉트(생성·편집)+숫자 팝 체크(기억·복원·초기화)
- [x] 검증: 유닛 4종(명령 필터·ASS 이벤트·실렌더 — 흑백 채도 소멸 90%↑·팝 구간
      밝은 픽셀 유/무 실측·run_job 추출/끔) + 서버 1종 + E2E 7체크 + 전체 284 테스트

### ✅ v0.57 — 초보자 첫 경험 대청소 (스스로 초보 워크스루 감사 → 사용자 승인)

- [x] 목소리 기본 🌟 AI 성우(gemini checked) — 키 없으면 생성 시점에
      ensureGeminiKey 프롬프트(발급 링크 포함), 취소 시 "키 없이 계속" 확인 후
      폴백 진행. Windows 내장 음성 자동 기본 지정 제거, 소리 점검용(구 테스트 톤)
      라디오는 ⚙️ 세부 설정 안으로 (name=prov 그룹 유지)
- [x] 「🎬 자동 연출」 그룹 신설 — 효과음(음악 그룹에서)·펀치 줌·숫자 팝·화면 톤
      (자막 그룹에서) 이동. id 불변이라 JS·기억 로직 그대로
- [x] 화면 정리: 🆕 v0.xx 표기 0건, 문단 힌트 1줄화(마스코트·내가 넣기·v0.45 설명),
      목소리 설명 3줄→1줄, 용어 통일(테스트 톤→소리 점검용 — 배지·설정 문구 포함)
- [x] 완료 화면 "🎯 다음엔 이것도 해보세요" (썸네일→키트→검토 모드)
- [x] 검증: 구조 테스트 1종(기본값·그룹·표기 0건·안내) + E2E 8체크(무키 첫 실행
      프롬프트→계속→완성, 점검용 무프롬프트, 연출 그룹 4종) + 전체 285 테스트

### ✅ v0.57.1~v0.57.2 — 실사용 후속 패치 (사용자 리포트)
- [x] v0.57.1 사진 모드 다중 선택 — pick_path('images')=askopenfilenames,
      세미콜론 조인(resolve_photo_inputs 형식·순서 보존), [🖼 사진 고르기]+[📁 폴더째]
- [x] v0.57.2 장면 검토 통합 복사 — copyScenePrompts를 「N번 장면 ↵ 프롬프트 ↵ 빈 줄」
      블록 형식으로, 복사 내용을 sceneAllText 상자에 상시 표시(수동 복사 대비)
- [x] v0.57.2 장면 그리드 재디자인 — minmax 240px, 「장면 N」 배지 + 🟢/⬜ 상태,
      그림 없으면 <img> 미생성(깨진 아이콘 제거), 프롬프트 칸 96px
- [x] v0.57.2 홈 화면 누수 수정 — v0.57에서 효과음 블록 이동 때 남은 여분 </div>가
      #formCard를 조기 종료시켜 AI 배경 그림·세부 설정·시작 버튼이 첫 화면에
      노출되던 회귀 (div 균형 검사 추가로 검출)
- [x] 검증: E2E 28체크(그리드·복사 형식·폴더 삽입·완성) + webui 44 테스트

### ✅ v0.60 — 꾸미기 시각화 (사용자 피드백: "글씨만으론 효과를 모른다")
- [x] 자막 스타일: select 숨기고 견본 칩 4종 — 칩 자체를 프리셋 모양으로 렌더
      (outline text-shadow / 노랑 / 흰 띠 pill / 네온 글로우), pickStyleChip→select 동기화
- [x] 효과음: GET /sfx/{pop|whoosh|ding} (ensure_sfx 보장 후 서빙, 이름 화이트리스트)
      + ▶ 듣기 버튼 3개 (사용자 교체 파일도 그대로 재생됨)
- [x] 펀치 줌·인포 팝: .fx-demo CSS 미니 화면 (punchD scale 1→1.28 / popD 50% 팝 루프)
- [x] 화면 톤: 같은 그라데이션 썸네일에 CSS filter 5종(sepia틸·brightness·contrast·
      grayscale) 스와치 — pickToneCard→select 동기화, 양 폼 공통
- [x] 동기화: syncDecorChips()를 초기 복원·applyEditLast·resetGenForm에 연결
      (설정 기억/템플릿 복원 시 칩·스와치 표시 일치)
- [x] 검증: E2E 23체크(칩 스타일 실측·SFX RIFF 서빙·애니메이션·복원) +
      webui 구조 테스트(경로 탈출 404 포함) — 사용자 지시대로 중간 배포 없이 일괄

### ✅ v0.59 — 내장 음성 보이스 선택 (사용자 요청 ㉰)
- [x] tts_engine: _SAPI_PS1에 보이스 선택(지정 → ko 첫 번째 → exit 3), build_sapi_script
      (따옴표 이스케이프), _SAPI_LIST_PS1 + list_windows_voices() (ko 우선 정렬,
      비Windows는 빈 목록), WindowsTTS(voice=) + cache_extra v: 반영,
      make_provider가 tts.windows_voice 전달, 플랫폼 오류 TTSNonRetryable로
- [x] webui: /api/win_voices, 설정 「내장 음성 목소리」 선택+미리듣기(_preview에
      windows_voice 주입), 저장 tts.windows_voice, 목록에 없는 저장값 (저장됨) 유지
- [x] config: tts.windows_voice 기본값 ""
- [x] 검증: 유닛 5(스크립트 치환·이스케이프·목록·캐시키·전달) + webui 1(API·저장·
      미리듣기 오류) + E2E 7체크 (실보이스 합성은 Windows 실PC에서만 가능)

### ✅ v0.58 — 🎤 직접 녹음한 내레이션 통째 넣기 (사용자 요청 ㉯)
- [x] 편집 폼 내레이션 그룹: narrMode 라디오(ai/file) + narrFile 픽커(audio) —
      녹음 선택 시 AI 박스 숨김, 시작 검증(파일 필수), 사진·영상 모드 공통
- [x] edit_mode.analyze_narration_file: 녹음 원본 타임라인 그대로 —
      대본 있으면 발화 구간 배치(비용 0), 없으면 구간별 STT(문장 타임스탬프)
- [x] 분석: 녹음 모드 시 원본 영상 STT 생략, 사진 길이=녹음 길이,
      완전 자동+긴 영상은 몽타주 목표=녹음 길이, 나누기·핵심선별·AI다듬기 배타
- [x] 렌더: 녹음 → apad/atrim으로 영상 길이 wav → narration_wav (TTS 경로와 동일
      믹스·덕킹), freeze/loop 연장·꼬리 다듬기 재사용, orig_audio 기본 무음
- [x] 검증: 유닛 4(구간 배치·무STT·가짜STT·무음), webui 1(검토 자막·길이·소리·
      실패 메시지), E2E 13체크(토글·검증·사진 6s·영상 12→6.4s·원본 무음)

### ✅ v0.57.3 — 최신 FFmpeg 렌더 즉사 수정 (실사용 리포트: Unrecognized option)
- [x] 원인: 1_설치.bat이 받는 BtbN master(FFmpeg 8.x)에서 -filter_complex_script
      제거(7.0 deprecated → 2025+ 삭제) → scene_slideshow가 항상 이 옵션 사용
- [x] utils.ffmpeg.filter_complex_args: 그래프 ≤20k자는 인라인 -filter_complex
      (전 버전 공통), 초과 시 파일 경유 — 빌드 도움말에서 지원 옵션 감지해
      -filter_complex_script(구형)/-/filter_complex(7.0+) 선택, 바이너리별 캐시
- [x] 적용 3곳: scene_slideshow(항상 파일이던 것), cut_video(>60구간 분기),
      photos_to_video(수백 장 시 32K 초과 잠복 버그)
- [x] 검증: FFmpeg 7.0.2 실바이너리로 신형 -/filter_complex 파일 경유 실렌더 +
      6.1.1 인라인/파일 실렌더 + 가짜 빌드 감지 유닛 6종 (compat 테스트 신설)

### Phase 2 — 자막 정확도·타이밍 개선
- [x] 2-1. Whisper **자체 문장 타임스탬프** 사용 (v0.41 — transcribe_timed, 구간당 문장별 자막)
- [x] 2-1b. (대체) AI 대본 다듬기로 오인식 일괄 교정 — v0.11
- [x] 2-2. Whisper 모델 선택(tiny~large-v3) — 정확도↑ (편집 폼) — v0.18
- [x] 2-3. 자막 최대 글자수·자동 줄바꿈(2줄 이내) — v0.17 + v0.34(초과분 자동 분할로 완결)
- [ ] 2-4. 초벌 자막 신뢰도 낮은 줄 표시(빨간 밑줄) → 수정 유도

### ✅ Phase 2.5 — 무내레이션 영상에 AI 내레이션 입히기 (v0.21, 사용자 요청)
> b-roll(식당·풍경 등 말 없는 영상)에 AI 대본+목소리를 얹어 완성. 두 모드의 결합.

- [x] 2.5-1. 편집 폼 "🎙️ AI 내레이션 추가" — 주제 입력 → Gemini(없으면 스텁) 대본
- [x] 2.5-2. 원본 소리 = 배경(0.15 덕킹), TTS = 목소리, 대본 = 자막(강조 포함)
- [x] 2.5-3. 대본을 영상 길이에 비례 배치(build_narration_wav로 자막 시각에 클립 배치)
- [x] 2.5-4. 대본을 검토·수정 화면(Phase 1 재사용)에서 손질 후 확정
- [x] 영상 루프/정지로 내레이션이 더 길 때 맞추기 (v0.42 — narr_fit freeze/loop/drop)

### ✅ 훅 제목 AI 추천 (v0.8, 완료)
- [x] 주제/키워드로 후킹 제목 여러 개 추천 → 클릭해서 상단 제목에 채우기 (양 모드)

### Phase 3 — 편집 모드 완성 요소 (캡컷 기능)
- [x] 3-1. **BGM 넣기** (편집 모드에도) — 볼륨·페이드 (v0.26. 사이드체인 덕킹은 추후)
- [ ] 3-2. **속도 조절** (0.5~2배) — 영상 setpts + 오디오 atempo, 자막 타이밍 자동 보정
- [x] 3-3. **로고 오버레이(워터마크)** — 크기·위치·투명도 (v0.35)
- [x] 3-4. 앞뒤 **트림**(시작/끝 지점 지정) — v0.41 검토 화면 ⏮/⏭

### Phase 4 — 롱폼(16:9) + 포맷
- [ ] 4-1. 가로 롱폼(16:9) 출력 모드
- [ ] 4-2. 세로/가로 자동 변환 품질 개선(블러 배경 or 스마트 크롭)
- [ ] 4-3. 챕터/구간 마커 (긴 영상 관리)

### Phase 5 — 완성형 폴리시
- [ ] 5-1. 컷 사이 **전환 효과**(페이드/디졸브)
- [ ] 5-2. **템플릿**(제목·자막·색 프리셋 저장/불러오기)
- [ ] 5-3. 강조 자막 애니메이션(팝/타이핑)
- [ ] 5-4. 인트로/아웃트로

### ~~Phase 6 — 캡컷 draft~~ (v0.41에서 기능 자체를 제거 — 사용자 결정 "캡컷 기능 모두 빼줘")

---

## 아키텍처 메모 (완성형으로 가려면)

- 지금 UI는 "입력→클릭→출력" 1회성. 편집기는 "생성 → **수정** → 미리보기 → 내보내기"
  흐름이라 **편집 화면(Phase 1)** 이 그 전환점.
- 자막은 항상 job 폴더에 **.srt + .json**으로 저장 → 언제든 다시 열어 수정/재렌더.
- 각 단계는 독립적으로 배포 → 사용자가 매 단계 실제로 써보고 피드백.

## 현실적 한계 (솔직히)

- 캡컷·브루의 **타임라인 드래그 편집, 키프레임, 수백 개 스티커/효과**는 브라우저 기반
  경량 도구로 완전 재현하기 어렵다. → 컷대장은 "자동화 + 핵심 수정"에 집중,
  세밀한 손질은 캡컷 draft 내보내기로 위임하는 하이브리드가 현실적.
- 자막 100% 정확은 어떤 STT도 불가 → **빠른 수정 UX**가 정답 (Phase 1).
