# 모바일 숏폼 자동 제작 앱 — 기술 조사 보고서

> 작성일 2026-06-24 · 딥리서치(웹검색→소스수집→3표 적대적 검증) + 도메인 지식 종합
> 가격/모델 버전은 **2026년 6월 기준**이며 변동이 빠릅니다. 구축 전 각 공급자 페이지 재확인 필수.
> 표기: `[검증됨]` = 리서치에서 출처·교차검증 완료 / `[업계관행]` = 1차 검증 부족, 일반 지식 기반 추정

---

## 0. 핵심 결론 (TL;DR)

1. **"고퀄리티 자동 쇼츠"는 두 가지 다른 제품이다.** 어느 쪽을 만들지부터 정해야 한다.
   - **A. 0부터 생성형(Faceless/AI 생성)**: 주제 → AI 대본 → TTS 음성 → AI 이미지·영상/스톡 B-roll → 자막 번인 → 렌더. (예: "이런 사실 알았어?" 류 정보성 채널)
   - **B. 롱폼 재가공형(Opus Clip식)**: 10~60분 영상 입력 → AI가 바이럴 구간 탐지 → 9:16 세로로 5~20개 클립 자동 추출. (예: 팟캐스트·강의 클립화)
2. **검증된 오픈소스 청사진이 이미 존재한다** — 바닥부터 만들지 말고 이걸 베이스로 시작하라.
3. **모바일 온디바이스 렌더링은 지뢰밭이다.** 업계 표준이던 `ffmpeg-kit`이 **2025년 6월 23일 공식 중단**됐다 `[검증됨]`. → 무거운 렌더링은 **서버에서** 하고, 폰은 입력·미리보기·업로드만 담당하는 구조가 현실적.
4. **MVP 권장**: 모바일 앱(촬영/입력/미리보기) + 클라우드 렌더 백엔드(FFmpeg) + 교체 가능한 AI 공급자 레이어.

---

## 1. 자동 생성 파이프라인 (7단계) `[검증됨]`

오픈소스 `youtube-shorts-pipeline`, `OpenShorts` 등 복수 프로젝트가 동일 구조를 독립적으로 재현 → 사실상 업계 표준 흐름:

| 단계 | 내용 | 대표 도구 |
|---|---|---|
| 1. 리서치/소스 | 주제·소재 수집 (재가공형은 생략 가능) | 웹스크래핑, 트렌드 API |
| 2. 대본 생성 | LLM이 후킹 멘트·스크립트 작성 | Claude / GPT / Gemini |
| 3. 비주얼 | AI 이미지 또는 AI 영상 또는 스톡 B-roll | Flux, Runway, Pexels |
| 4. 음성(TTS) | 대본을 자연스러운 내레이션으로 | ElevenLabs |
| 5. 자막 | **Whisper로 단어 단위 타임스탬프** → SRT/ASS | Whisper / WhisperX |
| 6. 조립/렌더 | **FFmpeg**: Ken Burns 줌·팬, BGM, 자동 보이스 더킹, 자막 번인 | FFmpeg |
| 7. 업로드 | 플랫폼 배포 | YouTube/TikTok API |

**핵심 설계 원칙 `[검증됨]`**: LLM·TTS 공급자를 **교체 가능하게 추상화**(provider-agnostic)하라. 검증된 오픈소스도 Claude/Gemini/GPT/Ollama, ElevenLabs/Edge TTS/MiniMax를 런타임에 바꿀 수 있게 설계돼 있다. 품질↔비용 트레이드오프를 위해 필수.

### 재가공형(Opus Clip식) 세부 단계 `[검증됨]`
다운로드(yt-dlp) → Whisper 전사 → LLM 콘텐츠 분류 → 청킹(20분 오버랩) → **LLM 바이럴 점수 0~100 랭킹** → 중복 제거(>50% 오버랩) → 9:16 세로 크롭·화자 추적 리프레이밍(YOLOv8/MediaPipe) → 렌더.

---

## 2. 단계별 API / 가격 (2026-06 기준)

### LLM (대본·자막·바이럴 랭킹) `[검증됨]`
| 모델 | 입력 ($/1M토큰) | 출력 ($/1M토큰) | 용도 |
|---|---|---|---|
| Claude Sonnet 4.6 | $3 | $15 | 프로덕션급 대본 품질 |
| **Claude Haiku 4.5** | **$1** | **$5** | 대량 배치 — 현세대 최저가 |
| Claude Opus 4.x | $5 | $25 | 최고 난도 |

> 대본 1편은 토큰이 적어(수천 토큰) 실제 LLM 비용은 영상당 보통 1센트 미만. 비용 병목은 LLM이 아니라 **영상 생성·TTS**다.

### TTS (ElevenLabs) `[검증됨]`
| 플랜 | 월 요금 | 분량 | 비고 |
|---|---|---|---|
| Starter | $5 | ~30분 | 상업 라이선스 + 인스턴트 보이스 클로닝 |
| Creator | $22 | ~100분 | 프로 보이스 클로닝(PVC), 192kbps |
| Pro | $99 | ~500분 | API로 44.1kHz PCM 프로덕션급 |

**비용 절감 `[검증됨]`**: 표준 Multilingual v2는 1글자=1크레딧이지만 **Flash·Turbo v2.5는 0.5크레딧/글자** → 동일 크레딧으로 출력량 2배. 자동화 앱엔 Flash/Turbo 권장. (한국어 품질은 직접 테스트 필요)

### AI 영상 생성 (Runway) `[검증됨]`
- 개발자 포털 **$0.01/크레딧** (최소 $10 = 1,000크레딧)
- 모델별 초당 크레딧: Gen-4 Turbo 이미지 2 ~ **Gen-4.5 영상 25크레딧/초(≈$0.25/초)**, Gen-4 Turbo 영상 ~5크레딧/초
- ⚠️ 주의: API 가격 페이지(25크레딧/초)와 헬프 문서(12크레딧/초)가 상충 — 미디어/티어 차이로 보임. **Gen-4.5 영상 단가는 직접 확인 필수**

### 영상 생성 모델 라우팅 — 실전 패턴 `[검증됨]`
검증된 오픈소스(OpenShorts)는 Runway/Pika/Sora를 직접 부르지 않고 **fal.ai를 통해 여러 모델로 라우팅**한다:
- 이미지: **Flux 2 Pro** / 영상: **Hailuo 2.3** / 립싱크 아바타: **Kling Avatar v2**, VEED
- → **fal.ai 같은 통합 게이트웨이**를 쓰면 모델을 한 인터페이스로 갈아끼울 수 있어 유리.

### ⚠️ 검증 안 된 항목 (직접 확인 필요)
- **Sora(OpenAI) / Pika / Kling 공식 API 가용성·가격** — 리서치에서 확정 못함
- **스톡 영상**: Pexels API(무료, 출처표기 권장), Storyblocks(구독제) — 가격·라이선스 미검증. `[업계관행]` Pexels/Pixabay는 무료·상업이용 가능이 일반적이나 **자동화 대량사용은 약관 재확인 필수**
- **영상당 실측 종단 비용** — 리서치의 비용 벤치마크 클레임들은 **모두 반증(refuted)**되어 신뢰 불가. 프리미엄 스택(Claude+ElevenLabs+Runway)으로 60~90초 1편 단가는 **직접 측정** 필요. (대략 AI영상을 많이 쓰면 영상당 수백 원~수천 원대로 추정되나 미검증)

---

## 3. 모바일 기술 스택 — 가장 중요한 제약

### `ffmpeg-kit` 중단 사태 `[검증됨, 강력]`
- React Native/모바일에서 가장 널리 쓰이던 FFmpeg 바인딩 `ffmpeg-kit`이 **2025-06-23 저장소 아카이브(읽기전용)**, **2025-04-01자로 Maven/CocoaPods/npm 바이너리 제거**.
- 메인테이너가 "Saying Goodbye to FFmpegKit"(2025-01-06)로 공식 은퇴 발표 (MPEG LA의 Via-LA 인수 후 라이선스 불확실성).
- **결론: 온디바이스 FFmpeg 렌더링에 의존하지 마라.**

### 권장 아키텍처: 하이브리드 (서버 렌더) `[업계관행, 추천]`
```
[모바일 앱]                          [클라우드 백엔드]
 - 입력/주제 선택                      - LLM 대본 (Claude)
 - 영상 촬영/업로드          ──API──>  - TTS (ElevenLabs)
 - 실시간 미리보기(저해상도)            - AI영상/스톡 (fal.ai/Pexels)
 - 진행상황 표시                       - Whisper 자막
 - 결과 다운로드/공유        <──────    - FFmpeg 렌더 (서버, 고해상도)
```
- **이유**: 폰은 발열·배터리·메모리 한계로 4K/긴 영상 렌더에 부적합. 서버 FFmpeg는 빠르고 일관적이며 라이선스도 명확. AI 모델 호출도 어차피 서버가 안전(API 키 보호).
- **온디바이스가 필요한 경우**(오프라인·즉시성): 미리보기·간단 컷편집만 온디바이스, 최종 렌더는 서버.

### 프레임워크 선택 `[업계관행]`
| | React Native | Flutter |
|---|---|---|
| 영상 편집 생태계 | RN 생태계 풍부, JS 개발자 많음 | 렌더 성능·UI 일관성 우수 |
| 추천 | 웹/JS 자산 재사용 시 | 신규·고성능 UI 중시 시 |

> 어느 쪽이든 **무거운 렌더는 서버로** 가는 게 핵심이라 프레임워크 선택의 비중은 낮음. 팀이 익숙한 걸로.

### 떠오르는 대안: OpenCut (Rust 코어) `[검증됨]`
- MIT, ~59.4k 스타, CapCut 대안 표방. 데스크톱·모바일·브라우저를 **단일 Rust 코어(WASM/네이티브, wgpu 컴포지터)**로 배포 목표.
- ⚠️ 단 **모바일은 아직 로드맵 단계(미출시)**. 지금 당장은 못 씀. 장기 모니터링 대상.

---

## 4. 참조 오픈소스 청사진 `[검증됨]`

바닥부터 만들지 말 것. 아래를 포크/학습해서 시작:

| 프로젝트 | 라이선스 | 특징 |
|---|---|---|
| **OpenShorts** (mutonby) | MIT, Docker | **3-in-1**: 클립 생성기 + AI UGC(합성 액터) + YT 최적화. ~1,017줄 main.py, faster-whisper + Gemini 2.5 Flash 바이럴 탐지 + YOLOv8/MediaPipe 화자추적 + FFmpeg, FastAPI/Python 3.11 |
| **youtube-shorts-pipeline** (rushindrasinha) | — | 7단계 CLI 로컬 파이프라인, 다중 LLM/TTS 추상화의 교과서 |
| **ai-youtube-shorts-generator** (samuraigpt) | — | Opus Clip식 재가공형 레퍼런스 |
| **OpenCut** | MIT | 크로스플랫폼 편집기 코어 (모바일 미래) |

---

## 5. 경쟁 앱 & 차별화 `[업계관행]`

| 앱 | 강점 | 빈틈(기회) |
|---|---|---|
| CapCut | 수동 편집 끝판왕, 템플릿 | 자동 "주제→완성본"은 약함 |
| Opus Clip | 롱폼 재가공 1등 | 0부터 생성 X, 모바일 우선 X |
| Submagic | AI 자막·하이라이트 | 풀 자동 생성 파이프라인 X |

**차별화 포인트 후보**:
1. **"모바일 우선 + 완전자동"** — 경쟁자 대부분 데스크톱/웹 중심. 폰에서 주제 한 줄 → 완성 쇼츠.
2. **한국어 특화** — 한국어 TTS 음색·자막 스타일·밈/트렌드 최적화 (글로벌 툴의 약점).
3. **니치 템플릿** — 특정 카테고리(뉴스/리뷰/명언 등) 전용 고퀄 템플릿.
4. **워터마크 없음 + 합리적 가격**.

---

## 6. MVP 권장 로드맵

**1단계 (검증)**: 재가공형 1종만. 모바일 앱 = 업로드+미리보기, 서버 = `ai-youtube-shorts-generator` 베이스(Whisper+LLM랭킹+FFmpeg 세로크롭). AI 영상생성 없이 시작 → 비용 0에 가깝게.

**2단계 (고퀄)**: 0부터 생성형 추가. Claude 대본 + ElevenLabs(Flash) TTS + Pexels B-roll + Whisper 자막 + 서버 FFmpeg. AI 영상은 fal.ai 게이트웨이로 옵션 제공.

**3단계 (차별화)**: 한국어 음색/템플릿, 자동 업로드, 구독 과금.

**기술 스택 요약**:
- 모바일: React Native 또는 Flutter (팀 친숙도 기준)
- 백엔드: Python(FastAPI) + FFmpeg + 작업 큐(Celery/Redis)
- AI 레이어: Claude(LLM) + ElevenLabs(TTS) + Whisper(자막) + fal.ai(영상) + Pexels(스톡) — **전부 어댑터로 추상화**
- 렌더: **서버사이드 FFmpeg** (온디바이스 회피)

---

## 7. 다음 직접 확인 필요 항목 (리서치 미해결)
- [ ] Sora/Pika/Kling 공식 API 가격·가용성
- [ ] Pexels/Storyblocks 자동화 대량사용 라이선스 약관
- [ ] 60~90초 1편 실측 종단 비용 (프리미엄 스택)
- [ ] 한국어 TTS 품질 실측 (ElevenLabs vs 대안)
- [ ] React Native vs Flutter 영상 미리보기 성능 실측
